## How to install pip and libraries:

### Creating and activating venv
```bash
python -m venv venv

.\venv\Scripts\activate
```

### Installing libraries
```bash
pip install openai  

pip install python-dotenv

pip install Flask

pip install flask-cors
```